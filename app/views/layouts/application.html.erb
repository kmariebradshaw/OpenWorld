<!DOCTYPE html>
<html>
  <head>
<link href="https://fonts.googleapis.com/css?family=Open+Sans:400,800|Oswald:400,600" rel="stylesheet">

    <title>Open World</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav>
      <ul>
      <% if current_user %>
        <li><%= link_to "home", "/" %></li>
        <li><%= link_to "profile", "/users/#{current_user.id}"%></li>
        <li><%= link_to 'new post', new_portal_path, :class=>"add_link" %></li>

      </ul>
      <% end %> 
    </nav>
  	  <p class="notice"><%= notice %></p>
       <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
